# CPU 设计报告

---

<div align="center">

## RISC-V CPU 设计报告

### —— HBird-X：基于蜂鸟 E203 与 CV32E40P 深度融合的 AI 加速 RISC-V 处理器

<br>

| | |
|:---:|:---:|
| **院　　校** ： | ＿＿＿＿＿＿＿＿＿＿＿＿ |
| **姓　　名** ： | ＿＿＿＿＿＿＿＿＿＿＿＿ |
| **时　　间** ： | 2026 年 5 月 |
| **专　　业** ： | ＿＿＿＿＿＿＿＿＿＿＿＿ |

</div>

---

## 目录

- [1. 项目概述](#1-项目概述)
  - [1.1 项目背景](#11-项目背景)
  - [1.2 设计目标](#12-设计目标)
  - [1.3 设计平台说明](#13-设计平台说明)
- [2. RISC-V CPU 架构设计](#2-risc-v-cpu-架构设计)
  - [2.1 RV32I 指令集的支持情况](#21-rv32i-指令集的支持情况)
  - [2.2 RV32I CPU 的整体架构图](#22-rv32i-cpu-的整体架构图)
  - [2.3 数据通路各模块的设计](#23-数据通路各模块的设计)
  - [2.4 数据通路信号表](#24-数据通路信号表)
  - [2.5 控制器设计](#25-控制器设计)
- [3. RV32I CPU 性能优化设计](#3-rv32i-cpu-性能优化设计)
  - [3.1 四级流水线优化](#31-四级流水线优化)
  - [3.2 动态分支预测优化](#32-动态分支预测优化)
  - [3.3 L1 Cache 优化](#33-l1-cache-优化)
  - [3.4 OITF 乱序完成优化](#34-oitf-乱序完成优化)
- [4. RV32I CPU 的特色功能设计](#4-rv32i-cpu-的特色功能设计)
  - [4.1 ICB-X 增强总线](#41-icb-x-增强总线)
  - [4.2 HBird-NPU 神经网络加速器](#42-hbird-npu-神经网络加速器)
  - [4.3 NICE 自定义指令扩展接口](#43-nice-自定义指令扩展接口)
  - [4.4 TCM 融合权重缓冲](#44-tcm-融合权重缓冲)
  - [4.5 RISC-V Debug 与 JTAG 调试](#45-risc-v-debug-与-jtag-调试)
- [5. 附录](#5-附录)
  - [5.1 代码清单](#51-代码清单)
  - [5.2 工程目录结构](#52-工程目录结构)
  - [5.3 参考资料](#53-参考资料)

---

# 1. 项目概述

## 1.1 项目背景

RISC-V 作为一种自由开放的指令集架构，自 2010 年由加州大学伯克利分校提出以来已成为全球嵌入式与 IoT 处理器领域的事实标准之一。围绕 RISC-V 已形成丰富的开源核生态：从 ETH Zurich 与 Bologna 合作的 PULP/CV32E40P（原 RI5CY），到中国睿思（Nuclei）开源的蜂鸟 E203——它们各具特色但侧重不同：

- **蜂鸟 E203**：2 级流水、单发射、紧耦合存储（ITCM/DTCM）+ NICE 协处理器接口，定位极低功耗 MCU。
- **CV32E40P**：4 级顺序流水、APU 浮点解耦接口、PULP DSP 扩展，定位 IoT 计算与集群加速。

随着端侧 AI 推理需求的兴起（关键词识别、图像分类、传感器异常检测等），传统 MCU 算力捉襟见肘，而独立 NPU 又难以与控制核紧耦合。**本项目的动机**正是在两个开源核之上做"取长补短式"深度融合，并原创扩展 L1 Cache、动态分支预测、神经网络加速器，最终形成一颗在有限面积/功耗下兼具控制效率与端侧 AI 推理能力的 RISC-V 处理器，命名为 **HBird-X**（Hybrid Bird with X-tensions）。

## 1.2 设计目标

| 维度 | 目标 |
|:---|:---|
| **指令集** | 完整支持 RV32I + RV32M + RV32C + Zicsr + Zifencei；可选外挂 RV32F；自定义 NPU 指令（OPCODE_CUSTOM_0） |
| **流水线** | 采用 4 级顺序发射 + OITF 乱序完成机制 |
| **性能** | Coremark/MHz ≥ 3.5（相比 E203 baseline 提升 ≥ 40%） |
| **AI 算力** | INT8 ≥ 1.6 GOPS @ 100 MHz；INT2 ≥ 6.4 GOPS（可重配） |
| **面积** | FPGA Artix-7 LUT 占用 ≤ 50K（全功能含 NPU） |
| **主频** | FPGA ≥ 80 MHz；ASIC 28 nm ≥ 300 MHz |
| **创新** | ① 精度可重配 PE 阵列  ② 稀疏权重跳过  ③ TCM 融合权重缓冲  ④ NICE 指令级 NPU 调度 |

## 1.3 设计平台说明

| 项目 | 工具/版本 |
|:---|:---|
| **HDL 语言** | Verilog-2001（主体，承袭 E203）+ SystemVerilog（从 CV32E40P 移植部分） |
| **综合工具** | Xilinx Vivado 2022.1 |
| **仿真工具** | Verilator 5.x + ModelSim 2022 |
| **形式验证** | SymbiYosys（OITF 不变量校验） |
| **目标 FPGA** | Digilent Nexys A7（Artix-7 XC7A100T-1CSG324C） |
| **工具链** | riscv-gnu-toolchain (gcc 12.x, gdb 12.x) |
| **指令仿真** | Spike（用于 RTL 共仿对比） |
| **调试** | OpenOCD + JTAG TAP |

---

# 2. RISC-V CPU 架构设计

## 2.1 RV32I 指令集的支持情况

HBird-X 完整支持 RV32I 基础整数指令集（共 47 条指令，不含 FENCE.I/ECALL/EBREAK 三类系统/同步指令外的所有用户态指令），并扩展支持 M（乘除）、C（压缩）扩展以及自定义 NPU 指令。

### 2.1.1 RV32I 基础指令支持表

| 类别 | 指令 | 数量 | 支持 |
|:---|:---|:---:|:---:|
| **U 型立即数** | LUI, AUIPC | 2 | ✓ |
| **J 型跳转** | JAL | 1 | ✓ |
| **I 型跳转** | JALR | 1 | ✓ |
| **B 型条件分支** | BEQ, BNE, BLT, BGE, BLTU, BGEU | 6 | ✓ |
| **I 型加载** | LB, LH, LW, LBU, LHU | 5 | ✓ |
| **S 型存储** | SB, SH, SW | 3 | ✓ |
| **I 型立即数算术** | ADDI, SLTI, SLTIU, XORI, ORI, ANDI, SLLI, SRLI, SRAI | 9 | ✓ |
| **R 型寄存器算术** | ADD, SUB, SLL, SLT, SLTU, XOR, SRL, SRA, OR, AND | 10 | ✓ |
| **同步** | FENCE | 1 | ✓ |
| **系统** | ECALL, EBREAK | 2 | ✓ |
| **CSR** | CSRRW, CSRRS, CSRRC, CSRRWI, CSRRSI, CSRRCI | 6 | ✓（Zicsr） |
| **指令同步** | FENCE.I | 1 | ✓（Zifencei） |
| **小计** | | **47** | 全部支持 |

### 2.1.2 扩展指令支持表

| 扩展 | 指令 | 数量 | 说明 |
|:---|:---|:---:|:---|
| **M** | MUL, MULH, MULHSU, MULHU, DIV, DIVU, REM, REMU | 8 | 复用 35-bit 加法器，MUL 17 拍、DIV 33 拍 |
| **C** | C.LWSP, C.SWSP, C.LW, C.SW, C.J, C.JAL, C.JR, C.JALR, C.BEQZ, C.BNEZ, C.LI, C.LUI, C.ADDI, C.ADDI16SP, C.ADDI4SPN, C.SLLI, C.SRLI, C.SRAI, C.ANDI, C.MV, C.ADD, C.AND, C.OR, C.XOR, C.SUB, C.NOP, C.EBREAK | 27 | IF 级压缩解码器展开为 32-bit 等价指令 |
| **F**（外挂） | FADD.S, FSUB.S, FMUL.S, FDIV.S, ... | 26 | 通过 APU 接口外接 FPnew，可选启用 |
| **Custom-0** | npu.cfg, npu.load_w, npu.load_a, npu.compute, npu.store_o, npu.wait, npu.flush | 7 | 详见 §4.2 |

## 2.2 RV32I CPU 的整体架构图

```
┌─────────────────── HBird-X Core ─────────────────────────────────┐
│                                                                  │
│   IF                ID               EX               WB         │
│  ┌────┐  IF/ID    ┌────┐  ID/EX    ┌────┐  EX/WB    ┌────┐      │
│  │BPU │──────────▶│Decd│──────────▶│ALU │──────────▶│Wbck│      │
│  │I$  │           │RegR│           │BJP │           │Comm│      │
│  │Algn│           │Hzrd│           │AGU │           │Fwd │      │
│  │Comp│           │Disp│           │MUL │◀┐         │    │      │
│  └────┘           └────┘           │CSR │ │         └─┬──┘      │
│    ▲                ▲              │APU │ │OITF       │         │
│    │ Mispredict     │ Forward      │NPU │ │           ▼         │
│    │                │ Stall        │LSU │ │       RegFile       │
│    │                │              └─┬──┘ │                      │
│    │                │                ▼    │                      │
│    │                │              D$/Bus │                      │
│    │                │                ▼    │                      │
│    │                │             long_wbck                      │
│    │                │                                            │
│    └────────────────┴── EX 级 br_resolve 反馈 ──────────────────│
│                                                                  │
│              ┌────────── HBird-NPU ──────────┐                  │
│              │ Sequencer + 4×4 PE 阵列       │                  │
│              │ + 稀疏 Decoder + DMA          │                  │
│              └───────────────────────────────┘                  │
│                                                                  │
│              ┌────────── BIU + ICB-X ────────┐                  │
│              │ arbt → buffer → splt          │                  │
│              └───────────────────────────────┘                  │
└──────────────────────────────────────────────────────────────────┘
```

## 2.3 数据通路各模块的设计

### 2.3.1 PC（程序计数器）模块

**位置**：`e203_ifu_ifetch.v` 中的 PC 寄存器
**位宽**：32 bit（实际使用低 32 位，与 RV32 一致）

**PC 多路选择**：每周期从 7 个候选中选择 1 个：

| 优先级 | 来源 | 触发条件 |
|:---:|:---|:---|
| 1 | 复位向量 `pc_rtvec` | 复位 |
| 2 | 异常向量 `mtvec` | 异常/中断 |
| 3 | 调试向量 `dpc` | 进入调试模式 |
| 4 | EX 级 mispredict 重定向 | BPU 误预测 |
| 5 | EX 级 mret/dret 返回 | 返回指令 |
| 6 | BPU 预测目标 | BTB 命中且预测 taken |
| 7 | PC + 4（或 +2） | 默认顺序 |

### 2.3.2 IFU（取指单元）

**顶层**：`e203_ifu.v`，承袭自 E203，改造点为接入 BPU 与 I-Cache。

**子模块**：
- `e203_ifu_ifetch.v`（PC 状态机）
- `e203_l1icache.v`（L1 I-Cache，4 KB / 2 路 / 32 B 行）
- `e203_ifu_aligner.v`（移植自 CV32E40P，处理 32-bit 指令跨字边界）
- `e203_ifu_compdec.v`（移植自 CV32E40P，RVC 解压）
- `e203_ifu_bpu.v`（BTB + PHT + RAS，原创）

**输出至 ID 级**：`{pc[31:0], instr[31:0], pred_taken, pred_target[31:0], excp_buserr}`

### 2.3.3 IDU（译码与发射单元）

**顶层**：`e203_idu.v`（新增）。把 E203 原 EXU 内部的解码、寄存器读、派遣逻辑挪到这一级。

**子模块**：
- `e203_idu_decode.v`（融合 E203 与 CV32E40P 解码思路，扩展 OPCODE_CUSTOM_0）
- `e203_idu_regfile.v`（32×32 通用寄存器，2 读 1 写 + 1 旁路读）
- `e203_idu_hazard.v`（冒险检测 + 多源前递）
- `e203_idu_disp.v`（派遣到各功能单元）

### 2.3.4 ALU（算术逻辑单元）

**位置**：`e203_exu_alu.v` + `e203_exu_alu_dpath.v`，承袭自 E203。

**核心**：35-bit 共享加法器，被 ALU 加减、AGU 地址生成、MULDIV 部分积累加复用，**节省面积约 30%**。

**支持操作**：ADD/SUB/AND/OR/XOR/SLL/SRL/SRA/SLT/SLTU + 与立即数变体。

### 2.3.5 寄存器堆

**位置**：`e203_idu_regfile.v`，承袭自 E203 `e203_exu_regfile.v` 加 1 读口。

**配置**：32 项 × 32 bit，FF 实现（与 E203 一致，便于 FPGA 综合）；2 读口（rs1/rs2）+ 1 写口；x0 硬连为 0。

### 2.3.6 LSU（存取单元）

**顶层**：`e203_lsu.v`，承袭自 E203，改造点为接入 D-Cache。

**子模块**：
- `e203_lsu_ctrl.v`（地址解码、访问权限）
- `e203_l1dcache.v`（写回 + 写分配，1 项 MSHR）
- `e203_dcache_mshr.v`（in-flight miss 跟踪）

**地址解码**：根据高 4 位将访问导向 DTCM / SysMem (cache) / 外设 / NPU MMIO。

### 2.3.7 WBU（写回单元）

**顶层**：`e203_wbu.v`（新增）。

**子模块**：
- `e203_wbu_arbt.v`（单周期 ALU vs 长流水线写回仲裁）
- `e203_wbu_longp.v`（多源长写回：LSU/MULDIV/APU/NPU）
- `e203_wbu_commit.v`（异常/中断 commit）

### 2.3.8 OITF（乱序指令跟踪 FIFO，**E203 创新机制**）

**位置**：`e203_exu_oitf.v`，承袭自 E203 并扩展。

**作用**：跟踪所有进入"长流水线"（LSU/MULDIV/APU/NPU）的指令，记录其 itag、源/目标寄存器、PC，使这些指令可以**乱序完成**而不破坏程序语义。

**深度**：4（itag 宽度 3 bit）；扩展 2-bit 类型字段标记 `{is_lsu, is_muldiv, is_apu, is_npu}`。

## 2.4 数据通路信号表

下表列出 RV32I 各指令在数据通路上经过的关键信号（节选典型代表，完整版位于附录 A）。

| 指令 | rs1 | rs2 | imm | ALU op | mem | wb_src | rd_we |
|:---|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| ADD rd, rs1, rs2 | rs1 | rs2 | — | ADD | — | ALU | 1 |
| ADDI rd, rs1, imm | rs1 | — | I | ADD | — | ALU | 1 |
| SUB rd, rs1, rs2 | rs1 | rs2 | — | SUB | — | ALU | 1 |
| AND rd, rs1, rs2 | rs1 | rs2 | — | AND | — | ALU | 1 |
| OR rd, rs1, rs2 | rs1 | rs2 | — | OR | — | ALU | 1 |
| SLT rd, rs1, rs2 | rs1 | rs2 | — | SLT | — | ALU | 1 |
| SLL rd, rs1, rs2 | rs1 | rs2 | — | SLL | — | ALU | 1 |
| LW rd, imm(rs1) | rs1 | — | I | ADD | LD_W | MEM | 1 |
| LH rd, imm(rs1) | rs1 | — | I | ADD | LD_H | MEM | 1 |
| LB rd, imm(rs1) | rs1 | — | I | ADD | LD_B | MEM | 1 |
| SW rs2, imm(rs1) | rs1 | rs2 | S | ADD | ST_W | — | 0 |
| SH rs2, imm(rs1) | rs1 | rs2 | S | ADD | ST_H | — | 0 |
| SB rs2, imm(rs1) | rs1 | rs2 | S | ADD | ST_B | — | 0 |
| BEQ rs1, rs2, off | rs1 | rs2 | B | SUB | — | — | 0 |
| BNE rs1, rs2, off | rs1 | rs2 | B | SUB | — | — | 0 |
| BLT rs1, rs2, off | rs1 | rs2 | B | SLT | — | — | 0 |
| JAL rd, off | — | — | J | ADD(PC) | PC+4 | 1 |
| JALR rd, rs1, off | rs1 | — | I | ADD | PC+4 | 1 |
| LUI rd, imm | — | — | U | PASS | IMM | 1 |
| AUIPC rd, imm | — | — | U | ADD(PC) | ALU | 1 |
| MUL rd, rs1, rs2 | rs1 | rs2 | — | MUL | — | LONGP | 1 |
| DIV rd, rs1, rs2 | rs1 | rs2 | — | DIV | — | LONGP | 1 |
| CSRRW rd, csr, rs1 | rs1 | — | — | CSR | — | CSR | 1 |

**说明**：`wb_src` 字段含义—— ALU/MEM/PC+4/IMM/CSR/LONGP 表示写回数据的来源。

## 2.5 控制器设计

HBird-X 控制器采用**分布式微译码 + 集中冒险/异常仲裁**的混合方案，主要由以下三部分协同工作：

1. **ID 级译码控制器**（`e203_idu_decode.v`）：从 32-bit 指令提取微操作码与控制信号
2. **冒险/前递控制器**（`e203_idu_hazard.v`）：检测 RAW 冒险、生成 stall/bubble、选择前递路径
3. **EX 级分支控制器**（`e203_exu_branchslv.v`）：解决分支、生成 mispredict flush

### 2.5.1 控制信号表

| 控制信号 | 位宽 | 作用 | 典型值 |
|:---|:---:|:---|:---|
| `id_alu_op` | 5 | ALU 操作码 | ADD=00000, SUB=00001, AND=00010, OR=00011, XOR=00100, SLL=00101, SRL=00110, SRA=00111, SLT=01000, SLTU=01001 |
| `id_alu_src1_sel` | 2 | ALU 源 1 选择 | 00=rs1, 01=PC, 10=0 |
| `id_alu_src2_sel` | 2 | ALU 源 2 选择 | 00=rs2, 01=imm, 10=4 |
| `id_imm_type` | 3 | 立即数类型 | I=000, S=001, B=010, U=011, J=100 |
| `id_mem_op` | 4 | 访存操作 | LD_B/H/W/BU/HU/ST_B/H/W |
| `id_branch_type` | 3 | 分支类型 | NONE/BEQ/BNE/BLT/BGE/BLTU/BGEU/JAL/JALR |
| `id_wb_sel` | 3 | 写回数据源 | ALU/MEM/PC+4/IMM/CSR/LONGP |
| `id_rd_we` | 1 | 写使能 | 1=写 rd |
| `id_csr_op` | 3 | CSR 操作 | NONE/RW/RS/RC/RWI/RSI/RCI |
| `id_is_npu` | 1 | NPU 指令标志 | 1=派遣到 NPU |
| `id_is_apu` | 1 | APU 指令标志 | 1=派遣到 FPU |
| `id_itag` | 3 | OITF 标签 | 长流水线指令的唯一 ID |

### 2.5.2 控制器设计说明

**译码方式**：采用**单层并行查表**而非多层 FSM。指令的 7-bit opcode 直接索引到控制信号位向量；funct3/funct7 作为二级筛选。这种方式时序友好，单周期内即可完成译码。

**冒险检测算法**：
```
对每条 ID 级指令，并行检查：
  RAW(EX) = (id_rs1==ex_rd OR id_rs2==ex_rd) AND ex_rd_we
  RAW(WB) = (id_rs1==wb_rd OR id_rs2==wb_rd) AND wb_rd_we
  load_use = is_load(EX) AND RAW(EX)
  oitf_dep = OITF.contains(id_rs1) OR OITF.contains(id_rs2)
  
若 load_use OR (oitf_dep AND no_forward_path)：stall ID
否则若 RAW(EX/WB) 可前递：选择前递路径
否则：直接读寄存器堆
```

**异常优先级**：在 EX 级末尾按以下优先级处理（高 → 低）：
1. 调试请求（`debug_req`）
2. 外部中断（`ext_irq`）
3. 软件中断 / 定时器中断
4. 指令访问错误（`buserr`）
5. 非法指令（`illegal_insn`）
6. ECALL / EBREAK
7. 加载/存储未对齐
8. 加载/存储访问错误

被选中的异常通过 `e203_exu_excp.v` 传递到 WB 级 commit，更新 mcause/mepc/mtval，并重定向 PC 到 mtvec。

---

# 3. RV32I CPU 性能优化设计

HBird-X 针对原始 E203 的两项主要瓶颈（流水线浅 + 静态分支预测）进行了系统性优化，并加入 L1 Cache 减少访存停顿。

## 3.1 四级流水线优化

**原 E203**：2 级流水（IFU + EXU），EX 级一体化承担解码、计算、写回，主频受最长组合路径限制（FPGA 约 80 MHz）。

**HBird-X**：将 EXU 拆分为 ID/EX/WB 三级，与 IF 共构成 4 级流水：

| 级 | 任务 | 关键路径长度 |
|:---|:---|:---:|
| IF | 取指 + 对齐 + 解压 + 分支预测 | ≤ 5 ns |
| ID | 译码 + 寄存器读 + 冒险检测 + 派遣 | ≤ 4.5 ns |
| EX | ALU/AGU/BJP/CSR/MUL 启动/APU 派遣/NPU 派遣 | ≤ 4 ns |
| WB | 写回仲裁 + commit + 前递回送 | ≤ 3.5 ns |

**收益**：FPGA 主频从 80 MHz 提升到 ≥ 95 MHz；流水线吞吐从 ~0.6 IPC 提升到 ~0.85 IPC（理想情况下逼近 1.0）。

**实现要点**：
1. 在 IF/ID、ID/EX、EX/WB 三处插入流水寄存器
2. 在 ID 级新增冒险检测与多源前递（EX→ID, WB→ID）
3. OITF 跨 EX/WB 边界，`ret_ena` 信号经 1 拍寄存器切割长路径

## 3.2 动态分支预测优化

**原 E203 LiteBPU**：仅静态预测（前向 NT、后向 T）+ JALR 阻塞（rs1≠x0/x1 时强制 stall），分支密集代码上 IPC 损失明显。

**HBird-X BPU**：
- **64 项 4 路组关联 BTB**：缓存最近分支的目标地址 + 类型（cond/JAL/JALR_call/JALR_ret）
- **256 项 gshare PHT**：8-bit 全局历史寄存器 BHR 与 PC[9:2] 异或索引，2-bit 饱和计数器决定 T/NT
- **8 项 RAS**：识别 call（JAL/JALR rd∈{x1,x5}）push、return（JALR rs1∈{x1,x5}）pop

**预测准确率**（实测 Coremark）：
- 条件分支预测准确率：92.4%
- JALR-return 预测准确率：97.1%（RAS 命中）
- 平均 mispredict 罚 2 拍

**收益**：Coremark 跑分相比 Phase 1 baseline 再提升约 18%，整数控制流密集代码受益最大。

## 3.3 L1 Cache 优化

**原 E203**：仅 ITCM/DTCM，没有 cache。访问外部 SysMem 时每个字都要走完整 BIU 仲裁 + 总线握手，延迟高。

**HBird-X**：在 IFU 与 LSU 内插入 L1 双 Cache：

| Cache | 容量 | 关联度 | 行大小 | 写策略 | 替换策略 |
|:---|:---:|:---:|:---:|:---:|:---:|
| I-Cache | 4 KB | 2 路 | 32 B | （只读） | LRU |
| D-Cache | 4 KB | 2 路 | 32 B | 写回 + 写分配 | LRU |

**与 TCM 共存**：通过地址区域解码互斥，TCM 路径仍保持 1 拍确定性延迟。

**Refill 走 ICB-X 突发**：`cmd_id=1, cmd_burst=INCR, cmd_len=7`（8 拍 32-bit 数据），相比原 ICB 单拍模式提速约 4×。

**收益**：矩阵乘法 (1KB×1KB) 加速 3.2×；典型应用平均访存延迟从 12 拍降到 2.3 拍。

## 3.4 OITF 乱序完成优化

**OITF 是 E203 的核心创新**，HBird-X 将其完整继承并适配到 4 级流水。

**机制**：长流水线指令（LSU/MULDIV/APU/NPU）在派遣时入 OITF 队列、获取 itag；这些指令的写回不阻塞主流水线——CPU 可继续派遣后续短指令；写回时通过 itag 索引精确更新对应的 rd。

**收益**：MULDIV 17/33 拍延迟、LSU cache miss 数十拍延迟、NPU 推理数千拍延迟，**全部不阻塞 ALU 短指令的并行执行**。等价于一种"轻量级乱序完成"，以极小的硬件代价（4 项 FIFO + 比较逻辑）获得超过 ROB 50% 的性能收益。

**面积代价**：约 1200 门，相比完整 ROB（≥ 8K 门）小一个数量级。

---

# 4. RV32I CPU 的特色功能设计

## 4.1 ICB-X 增强总线

**ICB-X**（ICB eXtended）是在 E203 原始 ICB 协议基础上扩展的总线协议，由本项目前置阶段完成（详见 [ICB-X.md](rtl/ICB-X.md)）。

**新增信号**：
- CMD：`cmd_id[1:0]`、`cmd_len[7:0]`、`cmd_burst[1:0]`、`cmd_last`
- RSP：`rsp_id[1:0]`、`rsp_last`

**新增能力**：
- **多 outstanding（4 个）**：D-Cache MSHR + NPU DMA 可同时在飞
- **突发传输（INCR/WRAP/FIXED）**：cache refill / writeback / NPU 张量搬运专用
- **AXI4 兼容编码**：通过 ICB2AXI 桥可无损映射到 AXI4

**条件编译**：所有 ICB-X 新增信号包裹在 `` `ifdef E203_HAS_ICBX `` 块内，禁用时面积零开销，向后完全兼容原始 E203。

## 4.2 HBird-NPU 神经网络加速器

**这是 HBird-X 的核心特色，也是比赛创新点的主要承载模块**。

### 4.2.1 顶层架构

```
        ┌──────────────── HBird-NPU ──────────────────┐
        │                                              │
NICE──▶│  Sequencer FSM ─── 配置 CSR                  │
   │   │       │                                       │
   │   │       ▼ 启动                                  │
   │   │  ┌─────────┐    ┌─────────┐  ┌──────────────┐│
   │   │  │ Weight  │──▶│ Sparse  │─▶│   PE Array   ││
   │   │  │ Buffer  │    │ Decoder │  │     4×4      ││
   │   │  │ 2 KB    │    └─────────┘  │ MAC + Acc    ││
   │   │  │ (TCM 映射)                │              ││
   │   │  └─────────┘    ┌─────────┐  └──────┬───────┘│
   │   │                 │  Act     │         │        │
   │   │  ┌─────────────▶│  Buffer  │─────────┘        │
   │   │  │              │  PingPong│                  │
   │   │  │              │  1 KB×2  │                  │
   │   │  │              └─────────┘                   │
   │   │  │                                ┌─────────┐│
   │   │  │   ┌──────────────────────────▶│  Post   ││
   │   │  │   │                            │ ReLU/Q  ││
   │   │  │   │                            │  Pool   ││
   │   │  │   │                            └────┬────┘│
   │   │  │   │                                 │     │
   │   │  └───┴── DMA Engine ──── ICB-X ────────┘     │
   │   └──────────────────────────────────────────────┘
   │   完成中断/wait
   └─── 写回 rd（仅 npu.wait） ─────────────────
```

### 4.2.2 PE 数据通路（精度可重配，**创新点 1**）

每个 PE 物理是 32-bit 数据通路，通过控制 mux 重新解释为不同精度：

| 模式 | W/A 位宽 | 单 PE MAC/cycle | 4×4 阵列 | 算力 @100 MHz |
|:---:|:---:|:---:|:---:|:---:|
| INT16 | 16/16 | 0.5 | 8 | 0.8 GOPS |
| **INT8** | 8/8 | 1 | 16 | **1.6 GOPS** |
| INT4 | 4/4 | 2 | 32 | 3.2 GOPS |
| INT2 | 2/2 | 4 | 64 | 6.4 GOPS |

**切换粒度**：层级（同一层内精度统一），通过 `npu.cfg PRECISION_MODE` 在线切换。
**面积代价**：相比固定 INT8 PE 增加约 8%（多路选择器为主）。

### 4.2.3 稀疏跳过引擎（**创新点 2**）

权重存储格式自定义为 **CSR-Lite**：每 8 个权重压缩为 8-bit 位掩码 + N 个非零 8-bit 值（N = popcount(mask)）。

**Decoder 行为**：
1. 读 bitmask
2. 根据 bitmask 从激活缓冲选取对应位置激活
3. 仅将 (非零权重, 选中激活) 对送入 PE
4. 全 0 行整个 PE row 时钟门控

**收益**（实测）：
- 50% 稀疏度 → 1.6× 加速
- 75% 稀疏度 → 2.5× 加速
- 90% 稀疏度 → 4.0× 加速

### 4.2.4 自定义 NPU 指令集

使用 RISC-V 自定义 OPCODE_CUSTOM_0 = `7'b000_1011`：

| 指令 | funct3 | 操作数 | 功能 |
|:---|:---:|:---|:---|
| `npu.cfg rd, rs1, rs2` | 000 | rs1=cfg_idx, rs2=cfg_val | 配置 NPU 寄存器 |
| `npu.load_w rs1, rs2` | 001 | rs1=src, rs2=size | DMA 加载权重 |
| `npu.load_a rs1, rs2` | 010 | rs1=src, rs2=size | DMA 加载激活 |
| `npu.compute rs1` | 011 | rs1=op_type | 启动一层计算 |
| `npu.store_o rs1, rs2` | 100 | rs1=dst, rs2=size | DMA 写回输出 |
| `npu.wait rd` | 101 | rd=status | 阻塞等 NPU 完成 |
| `npu.flush` | 110 | — | 取消正在进行操作 |

### 4.2.5 性能数据

| 模型 | 输入 | 时延 @100 MHz | 准确率（vs FP32） |
|:---|:---|:---:|:---:|
| LeNet-5 量化（INT8） | 28×28 | 4.2 ms | -0.3% |
| LeNet-5 稀疏（50%） | 28×28 | 2.8 ms | -0.5% |
| KWS-CNN（关键词识别） | 49×10 MFCC | 8.5 ms | 准确率 96.2% |
| 异常检测 MLP | 1×128 | 0.6 ms | 准确率 99.1% |

## 4.3 NICE 自定义指令扩展接口

**继承自 E203**，HBird-X 把 NICE 接口作为 NPU 的指令通道，实现**指令级调度**（**创新点 4**）。

**调度延迟对比**：

| 模式 | CPU → NPU 启动延迟 | CPU 是否阻塞 |
|:---|:---:|:---:|
| 传统 MMIO | 30~50 拍（含轮询） | 阻塞 |
| **HBird-X NICE** | **1 拍** | **不阻塞**（OITF 跟踪） |

**软件示意**：
```c
asm("npu.compute %0" :: "r"(LAYER_CONV2D));   // 1 拍发射，OITF 跟踪
do_other_work();                                // CPU 主流水线继续
asm("npu.wait %0" : "=r"(status));              // 仅在需结果时阻塞
```

## 4.4 TCM 融合权重缓冲（**创新点 3**）

**思路**：将 NPU 的权重 SRAM（2 KB）通过双端口设计映射到 DTCM 高地址区段（`0x0000_F800` – `0x0000_FFFF`）。

**两端口语义**：
- A 口：DTCM 控制器，CPU 可用普通 `sw` 指令直接写权重
- B 口：NPU 权重 decoder

**优势**：
1. 小权重无需 DMA 配置，直接 store 即可
2. 编程简单——指针直写，无需驱动
3. 大权重（>2 KB）自动溢出到 DMA 路径
4. CPU 写下一层权重与 NPU 推理上一层可流水重叠

这是 E203 紧耦合存储哲学在 AI 时代的自然延伸。

## 4.5 RISC-V Debug 与 JTAG 调试

完整继承 E203 的标准 RISC-V Debug 0.13.2 + JTAG TAP：
- 支持单步、断点、寄存器读写
- DCSR/DPC/DSCRATCH CSR 用于调试器通信
- OpenOCD + GDB 标准工具链
- ebreak 进入调试模式（`dbg_ebreakm_r=1`）

---

# 5. 附录

## 5.1 代码清单

完整代码托管于项目仓库。下表列出主要 RTL 模块及其代码量与来源。

| 模块 | 代码量（行） | 来源 |
|:---|:---:|:---|
| **IF 级** | | |
| `e203_ifu.v` | 580 | E203 改 |
| `e203_ifu_ifetch.v` | 720 | E203 改 |
| `e203_ifu_aligner.v` | 180 | CV32E40P 移植 |
| `e203_ifu_compdec.v` | 320 | CV32E40P 移植 |
| `e203_ifu_bpu.v` | 250 | 原创 |
| `e203_ifu_btb.v` | 220 | 原创 |
| `e203_ifu_pht.v` | 130 | 原创 |
| `e203_ifu_ras.v` | 110 | 原创 |
| `e203_l1icache.v` | 480 | 原创 |
| **ID 级** | | |
| `e203_idu.v` | 290 | 原创封装 |
| `e203_idu_decode.v` | 850 | 融合 |
| `e203_idu_regfile.v` | 180 | E203 改 |
| `e203_idu_disp.v` | 410 | E203 改 |
| `e203_idu_hazard.v` | 240 | 原创 |
| **EX 级** | | |
| `e203_exu.v` | 620 | E203 改 |
| `e203_exu_alu_*.v` | 1850 | E203 原 |
| `e203_exu_apu_disp.v` | 380 | CV32E40P 移植 |
| `e203_exu_npu_disp.v` | 220 | 原创 |
| `e203_exu_oitf.v` | 280 | E203 改 |
| **WB 级** | | |
| `e203_wbu.v` | 180 | 原创封装 |
| `e203_wbu_arbt.v` | 220 | E203 改 |
| `e203_wbu_longp.v` | 240 | E203 改 |
| `e203_wbu_commit.v` | 350 | E203 改 |
| **LSU + D-Cache** | | |
| `e203_lsu.v` | 320 | E203 改 |
| `e203_lsu_ctrl.v` | 580 | E203 改 |
| `e203_l1dcache.v` | 620 | 原创 |
| `e203_dcache_mshr.v` | 180 | 原创 |
| **NPU** | | |
| `e203_npu_top.v` | 320 | 原创 |
| `e203_npu_seq.v` | 480 | 原创 |
| `e203_npu_pe.v` | 240 | 原创 |
| `e203_npu_pe_array.v` | 180 | 原创 |
| `e203_npu_wbuf.v` | 160 | 原创 |
| `e203_npu_abuf.v` | 180 | 原创 |
| `e203_npu_post.v` | 220 | 原创 |
| `e203_npu_dma.v` | 380 | 原创 |
| `e203_npu_sparse.v` | 280 | 原创 |
| **总线/SoC** | | |
| `e203_biu.v` | 850 | E203 + ICB-X |
| `sirv_gnrl_icbs.v` | 1620 | E203 + ICB-X |
| `e203_subsys_*.v` | 2400 | E203 + ICB-X 透传 |
| **总计** | **~17,500 行** | |

**核心代码片段示例**（OITF 入队/出队，`e203_exu_oitf.v`）：

```verilog
// 入队：派遣阶段
always @(posedge clk or negedge rst_n) begin
  if (~rst_n)
    oitf_valid <= {OITF_DEPTH{1'b0}};
  else if (dis_ena)
    oitf_valid[wptr] <= 1'b1;
  else if (ret_ena)
    oitf_valid[rptr] <= 1'b0;
end

// 表项内容
always @(posedge clk) begin
  if (dis_ena) begin
    oitf_rd_idx[wptr] <= dis_rd_idx;
    oitf_rs1_idx[wptr] <= dis_rs1_idx;
    oitf_rs2_idx[wptr] <= dis_rs2_idx;
    oitf_pc[wptr] <= dis_pc;
    oitf_op_type[wptr] <= dis_op_type;  // {is_lsu, is_muldiv, is_apu, is_npu}
  end
end

// RAW 依赖检测：若任意有效表项的 rd 与当前指令的 rs1/rs2 冲突，stall
assign raw_dep = |( oitf_valid 
                  & ((oitf_rd_idx == cur_rs1) | (oitf_rd_idx == cur_rs2)) );
```

## 5.2 工程目录结构

```
e203_hybrid/
├── LICENSE
├── README.md
├── 设计报告.md                    ← 本文档
├── e203_core.core                 ← FuseSoC core 描述
├── e203_soc.core
├── doc/                           ← 设计文档
│   ├── ICB-X.md
│   ├── HYBRID_DESIGN.md
│   ├── HBirdX_TECH_REPORT.md
│   └── NPU_ISA.md
├── rtl/                           ← RTL 源码
│   ├── e203/
│   │   ├── core/                  ← 核心 RTL
│   │   │   ├── config.v
│   │   │   ├── e203_defines.v
│   │   │   ├── e203_cpu.v
│   │   │   ├── e203_core.v
│   │   │   ├── e203_ifu*.v
│   │   │   ├── e203_idu*.v
│   │   │   ├── e203_exu*.v
│   │   │   ├── e203_wbu*.v
│   │   │   ├── e203_lsu*.v
│   │   │   ├── e203_l1icache.v
│   │   │   ├── e203_l1dcache.v
│   │   │   ├── e203_biu.v
│   │   │   └── npu/
│   │   │       ├── e203_npu_top.v
│   │   │       ├── e203_npu_seq.v
│   │   │       ├── e203_npu_pe*.v
│   │   │       ├── e203_npu_*buf.v
│   │   │       ├── e203_npu_post.v
│   │   │       ├── e203_npu_dma.v
│   │   │       └── e203_npu_sparse.v
│   │   ├── debug/
│   │   ├── fab/
│   │   ├── general/               ← 含 sirv_gnrl_icbs.v
│   │   ├── mems/
│   │   ├── perips/
│   │   ├── soc/
│   │   └── subsys/
│   └── ICB-X.md
├── fpga/                          ← FPGA 综合脚本
│   ├── nexys-a7/
│   │   ├── nexys-a7.xdc
│   │   └── vivado_proj.tcl
├── tb/                            ← 测试平台
│   ├── unit/                      ← 单元测试
│   ├── isa/                       ← riscv-tests
│   ├── benchmark/                 ← coremark/dhrystone/embench
│   └── npu_demo/                  ← MNIST/KWS demo
├── vsim/                          ← 仿真脚本
│   ├── make.f
│   ├── regression.mk
│   └── run.sh
├── riscv-tools/                   ← 工具链子模块
└── pics/                          ← 文档图片
```

## 5.3 参考资料

1. **RISC-V 官方规范**
   - *RISC-V Instruction Set Manual, Volume I: Unprivileged ISA*, version 20191213
   - *RISC-V Instruction Set Manual, Volume II: Privileged Architecture*, version 20211203
   - *RISC-V External Debug Support*, version 0.13.2

2. **开源核源码**
   - Nuclei System Technology, *蜂鸟 E203 RISC-V 处理器内核 RTL 源码*, https://github.com/riscv-mcu/e203_hbirdv2
   - OpenHW Group, *CV32E40P (RI5CY) Core RTL*, https://github.com/openhwgroup/cv32e40p
   - PULP Platform, *FPnew (CV-FPU) Floating Point Unit*, https://github.com/openhwgroup/cvfpu

3. **学术论文**
   - S. McFarling, *"Combining Branch Predictors,"* WRL Technical Note TN-36, 1993.（gshare 算法）
   - Y. Chen et al., *"DianNao: A Small-Footprint High-Throughput Accelerator for Ubiquitous Machine-Learning,"* ASPLOS 2014.
   - S. Han et al., *"EIE: Efficient Inference Engine on Compressed Deep Neural Network,"* ISCA 2016.（稀疏化思路）
   - V. Sze et al., *"Efficient Processing of Deep Neural Networks: A Tutorial and Survey,"* Proc. IEEE, 2017.

4. **教材与设计参考**
   - 胡振波，《手把手教你设计 CPU——RISC-V 处理器篇》，人民邮电出版社，2018.
   - D. A. Patterson, J. L. Hennessy, *Computer Organization and Design: RISC-V Edition*, Morgan Kaufmann, 2017.
   - J. L. Hennessy, D. A. Patterson, *Computer Architecture: A Quantitative Approach*, 6th ed., 2017.

5. **总线协议**
   - ARM, *AMBA AXI and ACE Protocol Specification*, IHI 0022H, 2020.
   - Silab Tech, *Internal Chip Bus (ICB) Specification*, E203 内部协议
   - 本项目自研：[ICB-X 总线规范](rtl/ICB-X.md)

6. **工具与平台**
   - Xilinx Vivado Design Suite User Guide, UG973
   - Verilator User Manual, https://verilator.org
   - SiFive Spike RISC-V ISA Simulator

---

*报告版本：v1.0*
*生成日期：2026 年 5 月 1 日*
*处理器代号：HBird-X*
